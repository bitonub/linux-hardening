# =============================================================================
#  hardening.conf — Optional configuration for harden.sh
#  Copy this file next to harden.sh and edit the values below.
# =============================================================================

# SSH port to use instead of default 22
SSH_PORT=2222

# Comma-separated list of additional ports to allow through the firewall
# Common examples: 80 (HTTP), 443 (HTTPS), 3000 (Node), 8080 (alt-HTTP)
ALLOWED_PORTS="80,443"
